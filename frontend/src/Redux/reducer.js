
const intitalState = {
    isAuth : false,
    isLoading : false,
    isError : false,
    user_data  : []
}

export const reducer = (state = intitalState, {type, payload}) => {
    switch(type){
        default :
         return state;
    }
}